# 그린존 산출물은 이 레포에 담지 않는다.
#
# 기능명세서는 개발 계획서 **원문**을 옮긴다 — 소스 파일 경로·줄번호·DB 컬럼명·운영 실측
# 수치가 그대로 실린다. 원장(ledger.json)이 과업 제목만 담는 것과 노출 규모가 다르다.
# 이 레포는 PUBLIC 이므로 기본은 담지 않고, 갈 곳은 export 가 지정하는 저장소 레포다.
greenzone/

# 로컬 시험 찌꺼기
.tmp*/
