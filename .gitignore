# 원장은 담지 않는다.
#
# 사람마다 자기 JEX 하네스의 과업을 봐야 하는데, 커밋하면 pull 한 사람이 **만든 사람의
# 과업**을 보게 되고, 각자 재생성한 것을 커밋하면 서로 덮어쓴다. 그래서 각자 만든다.
# 하네스가 없는 사람은 화면이 "원장을 아직 만들지 않았습니다" 안내를 띄운다.
web/ledger.json

# 로컬 시험 찌꺼기
.tmp*/
